SRCDIR?=..

OBJDIR = $(SRCDIR)/Objects/
vpath %.cpp .
vpath %.h   .:$(SRCDIR)/Base/
vpath %.o $(OBJDIR)

include $(SRCDIR)/Make.config

vpath %.$(EXT) $(SRCDIR):$(OBJDIR):.

INSTALL ?= install
INSTALL_PROGRAM = $(INSTALL)
INSTALL_DATA = ${INSTALL} -m 644

CXXLINK = $(CXX) -o $@

SHELL = /bin/sh
